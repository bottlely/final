<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="mars.content">
	<insert id="photoUpload" parameterType="HashMap">
		insert into myPage values
		(#{idx}, content_idx.nextval,1,sysdate,#{path},#{content},#{writer})
	</insert>
</mapper>