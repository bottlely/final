<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="mars.mtag">
   <insert id="add_Mtag" parameterType="HashMap">
      insert into mars_htag values(mars_mtag_idx.nextval, #{from_idx}, #{content_idx}, #{mtag}, #{member_idx}, sysdate,0,0,0,0)
   </insert>
</mapper>