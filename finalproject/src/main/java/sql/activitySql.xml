<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="mars.activity">
	<insert id="">
	
	</insert>
	
		<select id="active_like" resultType="String" parameterType="int">
		select name from mars_member ,(select from_idx from mars_reply where to_idx = #{idx})a where mars_member.idx = a.from_idx
	</select>
	
		<select id="active_reply" resultType="String" parameterType="int">
		select name from mars_member ,(select from_idx from mars_reply where to_idx = #{idx})a where mars_member.idx = a.from_idx
	</select>
</mapper>